// Generated by CoffeeScript 1.4.0
(function() {
  var selectTool;

  selectTool = {
    mouseDown: function(easel, eventType, mouseLoc, object) {
      var after, before, idx, selectedNodes;
      d3.mouse;
      if (eventType === "node") {
        selectedNodes = easel.pad.sketch.selectedNodes;
        idx = selectedNodes.indexOf(object);
        if (idx === -1) {
          return selectedNodes.push(object);
        } else {
          before = selectedNodes.slice(0, idx);
          after = selectedNodes(idx + 1);
          return easel.pad.sketch.selectedNodes = before.concat(after);
        }
      }
    },
    mouseUp: function(easel, eventType, mouseLoc, object) {
      return null;
    },
    mouseMove: function(easel, eventType, mouseLoc, object) {
      return null;
    }
  };

}).call(this);
